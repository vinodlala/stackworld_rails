<% content_for :title do  %>
  Edit a Room
<% end %>

<p>Here you can edit a room</p>

<%= simple_form_for @room, :html => {:class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :background_image %>
  <%= f.button :submit %>
<% end %>
